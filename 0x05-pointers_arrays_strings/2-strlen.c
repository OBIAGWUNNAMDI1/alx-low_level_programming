#include "main.h"
/**
 * _strlen - function that returns length of string
 * @s : a char value
 * Return:Always 0
 */
int _strlen(char *s)
{
	int p;

	for (p = 0; s[p] != 0; p++)
	{
	}
	return (p);
}
