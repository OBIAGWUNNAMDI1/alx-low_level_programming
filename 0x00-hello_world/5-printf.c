#include <stdio.h>

/**
 * main -print the string in the function
 * Description:using the main function
 * This program that prints with the printf
 * Return:0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
